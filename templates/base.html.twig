<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}COMPRA-VENTA CÓMICS Y NOVELAS GRÁFICAS{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block body %}

        {% block navbar %}
            {% include "comics/navBar.html.twig" %}
        {% endblock %}

        {# <div class="header">
            <div class="header__mess">
                <img src="{{ asset('comic (1).png', 'comic') }}">
                <a>Mensajes</a>
            </div>
            <div class="header__login">
                <a class="login" href="{{ path('app_login')}}">LOG IN</a>
            </div>
        </div>

        <div class="navbar">
            <a class="navbar__link" href="{{ path('home') }}">¿Cómo funciona?</a>
            <a class="navbar__link" href="{{ path('comic-list') }}">Comprar</a>
            <a class="navbar__link" href="{{ path('new-comic')}}">Vender</a>
        </div> #}

        {% endblock %}
    </body>
</html>
